# CS:APP 1장 퀴즈

- 범위: 1.1~1.10
- 난이도: 중하
- 문제 수: 5문제
- 답안은 별도의 `answer.md`에 작성되어 있습니다.


## 선택형
<p>Q1. 다음 설명 중 <b>옳은 것</b>을 모두 고르세요. </p>

1. 텍스트 파일은 비트로 구성되지 않으며, 바이너리 파일만 비트로 구성된다.
2. 같은 바이트열도 문맥에 따라 정수, 문자열 또는 기계어 명령으로 해석될 수 있다.
3. 전처리 → 컴파일 → 어셈블 → 링크 순서로 실행 파일이 만들어진다.
4. 어셈블러는 여러 목적 파일의 외부 심볼을 모두 해결하여 최종 실행 파일을 만든다.
5. 링커는 목적 파일과 라이브러리를 결합하고 심볼 참조를 해결해 실행 파일을 만든다.

---

<p> Q2. 다음은 사용자가 셸에서 `./hello`를 입력한 뒤 출력이 표시되기까지의 과정입니다. 
빈칸에 들어갈 말을 쓰세요.</p>

```text
디스크의 실행 파일
  → ( A )에 코드와 데이터가 적재됨
  → ( B )가 가리키는 주소의 명령을 CPU가 실행함
  → 출력 데이터가 ( A )에서 디스플레이 장치로 전달됨
```

---

<p> Q3. 다음 보기에서 일반적인 저장장치 계층의 순서를 <b>CPU에 가까운 곳부터 먼 곳</b>까지 올바르게 나열한 것을 고르세요. <p>

1. 레지스터 → L1 캐시 → L2 캐시 → L3 캐시 → 주기억장치 → 디스크
2. 레지스터 → 주기억장치 → L1 캐시 → L2 캐시 → L3 캐시 → 디스크
3. 디스크 → 주기억장치 → L3 캐시 → L2 캐시 → L1 캐시 → 레지스터
4. L1 캐시 → 레지스터 → L2 캐시 → 주기억장치 → L3 캐시 → 디스크

---


<p> Q4. 다음 설명에 해당하는 단어를 <보기>에서 하나씩 고르세요. 같은 보기는 여러 번 사용할 수 있습니다. </p>

> <보기> 
> 프로세스 / 스레드 / 가상메모리 / 파일

1. 각 실행 프로그램이 주기억장치를 독점해서 사용하는 것처럼 보이게 한다.
2. 키보드, 디스크, 디스플레이, 네트워크 같은 다양한 I/O 장치를 바이트의 연속으로 다루게 한다.
3. 실행 중인 프로그램이 프로세서와 시스템 자원을 사용하는 모습을 추상화한다.
4. 한 프로세스 안에서 코드와 전역 데이터를 공유하면서 서로 다른 제어 흐름을 실행한다.



## 서술형 

Q1. 문맥 전환(context switch)이 무엇인지 한 문장으로 설명하세요.

Q2. 캐시가 성능 향상에 도움을 주는 이유를 <b>시간 지역성</b> 또는 <b>공간 지역성</b> 중 하나를 사용하여 설명하세요.

Q3. 아래 보기를 읽고 부분 문제를 풀어보세요.
> 어떤 프로그램의 전체 실행 시간은 10초입니다. 그중 80%를 차지하는 부분을 병렬화하여 기존보다 4배 빠르게 만들었습니다.

3.1 개선 후 실행 시간을 계산하세요. <br>
3.2 전체 프로그램의 가속비(speedup)를 계산하세요. <br>
3.3 여러 코어가 해당 부분을 실제로 동시에 실행하여 처리 시간을 줄였다면 이것은 <b>동시성</b>과 <b>병렬성</b> 중 무엇을 성능 향상에 이용한 사례인지 쓰세요.
